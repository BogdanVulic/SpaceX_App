import styled from 'styled-components';

const StyledCrew = styled.div`
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    grid-gap: 1rem;
    justify-content: center;
    align-items: center;
`;

export default StyledCrew
